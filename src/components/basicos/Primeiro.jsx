import React from 'react';

export default function Primeiro(){
    const msg = 'Seja bem vindo(a) ao primeiro componente';
    return (
        <div>
            <h2>Primeiro Componente</h2>
            <p>{msg}</p>
        </div>
    )
}
